[project]
name = "jons-plan"
version = "0.1.0"
description = "Long-running agent harness for Claude Code"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.0.0",
]

[dependency-groups]
dev = []
